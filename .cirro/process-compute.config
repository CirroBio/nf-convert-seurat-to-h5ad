process {
    cpus = { task.attempt > 2 ? 8 : (task.attempt > 1 ? 2 : 1) }
    memory = { task.attempt > 2 ? '64 GB' : (task.attempt > 1 ? '16 GB' : '8 GB') }
    maxRetries = 2
    errorStrategy = "retry"
}